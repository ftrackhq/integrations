type: tool_config
name: standalone-file-loader
config_type: loader
compatible:
  entity_types:
    - FileComponent
  supported_file_extensions:
    - ".txt"
    - ".img"

engine:
  - type: plugin
    tags:
      - context
    plugin: resolve_entity_path
    ui: show_component
