type: tool_config
name: nuke-loader
config_type: loader
engine:

  # Component variants we are able to load
  - type: group
    tags:
      - component
    options:
      name: "Image loader"
      file_types:
        - ".png"
        - ".jpg"
        - ".jpeg"
        - ".exr"
        - ".tif"
        - ".tiff"
        - ".tga"
        - ".bmp"
        - ".hdr"
        - ".dpx"
        - ".mov"
    plugins:
      - type: plugin
        plugin: nuke_image_loader

  # Component variants we are able to load
  - type: group
    tags:
      - component
    options:
      name: "Sequence loader"
      component: sequence
    plugins:
      - type: plugin
        plugin: nuke_sequence_loader

