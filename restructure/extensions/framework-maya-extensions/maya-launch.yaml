integration: "git+https://github.com/user/integration-repo.git"

extensions:
 #- "ftrack-maya-default-extensions==1.0.0"
  #- "git+https://github.com/user/extension-repo.git"
  - "./local_extension_folder"

python-interpreter:[3.7, 3.10] #"./env/bin/python" #

environment_variables:
  PYTHONPATH:
    - $PYTHONPATH
    - '${source.dependencies}'
    - '${bootstrap_path}'
  MAYA_SCRIPT_PATH:
    - '${bootstrap_path}'
  FTRACK_MAYA_VERSION:
    - '${version}'

pre-launch-hook: setup_environment
launch-hook: launch_app # This is an optional key that overrides the default behaviour of how we launch the application.

