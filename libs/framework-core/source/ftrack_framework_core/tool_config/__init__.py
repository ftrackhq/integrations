# TODO: fix tool_configs:  update schemas to have description instead of name in the plugin section,
#  so we don't confuse it with the plugin name which is the current plugin key. Optimize
#  tool_configs and schemas, double check the utility of all the keys in tool_configs.
