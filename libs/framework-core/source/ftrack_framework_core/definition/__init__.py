# TODO: fix definitions:  update schemas to have description instead of name in the plugin section,
#  so we don't confuse it with the plugin name which is the current plugin key. Optimize
#  definitions and schemas, double check the utility of all the keys in definitions.
