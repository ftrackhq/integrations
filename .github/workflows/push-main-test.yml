name: Pytest

on:
  push:
    branches: [ "backlog/move-all-working-test-to-root" ]
  pull_request:
    branches: [ "main" ]

jobs:
  unit-test-pytest:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.7", "3.8", "3.9"]

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          python-version-attribute: "WHL"

      - name: Install poetry
        run: pip install poetry==1.4.2

      - name: Install dependencies
        run: poetry install
# TODO: when multiple python versions, try this to check if it improves speed. (Will have to remove the current poetry install)
#  We can implement coverage library as well to see where more tests are needed.
#      - name: Cache Poetry
#          uses: actions/cache@v2
#          with:
#            path: ~/.cache/pypoetry
#            key: ${{ runner.os }}-poetry-${{ hashFiles('**/poetry.lock') }}
#            restore-keys: |
#              ${{ runner.os }}-poetry-
#
#      - name: Install dependencies
#        run: poetry install --no-interaction --no-root

      - name: Run tests
        run: poetry run pytest