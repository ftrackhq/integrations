name: PR

on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - closed
      - ready_for_review
    branches: ["dummy"] # Disable CI - do not trigger, REMOVE THIS ON PR

jobs:
  check-pr:
    name: Check PR
    uses: ftrackhq/ftrack-actions/.github/workflows/pr-base.yml@main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
      ftrackApiKey: ${{ secrets.FTRACK_API_KEY }}
